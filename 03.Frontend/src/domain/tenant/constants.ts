import type { TenantId, LocationId, ContextVersion } from './types'

export const DEFAULT_COMPANY_ID: TenantId = 'company-default'
export const DEFAULT_LOCATION_ID: LocationId = 'location-default'
export const CONTEXT_VERSION: ContextVersion = 1
